# GAT-26 release image — EXACT pins from the validated inference environment
# (read from the training venv, 2026-07-23). Every line is fully pinned so the Docker
# build cannot silently upgrade/downgrade torch, CUDA-related packages, nnU-Net, or the
# reconstruction dependencies. torch is installed separately in the Dockerfile (torch==2.8.0 from
# the cu128 index) so the CUDA build is fixed. tests/test_release_env.py fails if any line here or
# any Dockerfile pip line is unpinned.
nnunetv2==2.8.1
acvl_utils==0.2.6
dynamic_network_architectures==0.4.4
batchgenerators==0.25.3
batchgeneratorsv2==0.3.5
einops==0.8.2
numpy==2.1.2
scipy==1.18.0
scikit-image==0.26.0
scikit-learn==1.9.0
nibabel==5.4.2
SimpleITK==2.5.5
pandas==3.0.3
imagecodecs==2026.6.26
tifffile==2026.7.14
